import * as TableActions from '@store/table/actions';
import { FeatureKey, reducer, State } from '@store/table/reducer';
import * as TableSelectors from '@store/table/selectors';

export { TableActions, FeatureKey, reducer, State, TableSelectors };
